import type { Problem } from '@/types/course'

// Сборник задач: Базы данных, часть 2
export const problems: Problem[] = [
  {
    type: 'quiz',
    id: 'b1',
    difficulty: 'medium',
    question:
      'Почему запрос `SELECT user_id, status, COUNT(*) FROM orders GROUP BY user_id` завершается ошибкой в PostgreSQL?',
    options: [
      'COUNT(*) нельзя сочетать с обычными колонками в одном SELECT',
      'GROUP BY принимает только одну колонку, а в SELECT их две',
      'Колонка status не входит в GROUP BY и не обёрнута в агрегат — база не знает, какое из значений группы выводить',
      'Ошибки нет: PostgreSQL молча выведет первое попавшееся значение status',
    ],
    correctIndex: 2,
    explanation:
      'После группировки каждая строка результата представляет целую группу, поэтому в SELECT допустимы только колонки из GROUP BY и агрегаты по остальным. У одного user_id может быть несколько разных status, и СУБД не вправе выбрать «правильный» сама: либо добавьте status в GROUP BY, либо примените к нему агрегат (MIN, MAX и т.п.).',
  },
  {
    type: 'code',
    id: 'b2',
    difficulty: 'medium',
    title: 'Поиск дубликатов',
    description:
      'Из-за бага в приложении в таблице `users(id, email)` появились повторяющиеся email. Напишите запрос, который выведет каждый email, встречающийся более одного раза, и число повторов — от самых частых к редким.',
    language: 'sql',
    starterCode: `-- TODO: найдите email, встречающиеся более одного раза,
-- и количество повторов; отсортируйте по убыванию количества
SELECT ...`,
    hints: [
      'Сгруппируйте строки по email и посчитайте размер каждой группы через COUNT(*).',
      'Фильтровать группы по результату агрегата нужно в HAVING, а не в WHERE — WHERE работает до группировки.',
      'Скелет: `GROUP BY email HAVING COUNT(*) > 1 ORDER BY ... DESC`.',
    ],
    solution: `SELECT email, COUNT(*) AS cnt
FROM users
GROUP BY email
HAVING COUNT(*) > 1
ORDER BY cnt DESC;`,
  },
  {
    type: 'quiz',
    id: 'b3',
    difficulty: 'medium',
    question:
      'Нужно склеить результаты двух SELECT — из основной и архивной таблиц заказов. Известно, что пересечений между ними нет. Какой вариант лучший и почему?',
    options: [
      'UNION — он надёжнее, потому что дополнительно проверяет корректность данных',
      'UNION ALL — он не тратит время на поиск и удаление дубликатов',
      'Разницы нет: оптимизатор сам поймёт, что дубликатов не бывает',
      'JOIN — объединять данные из двух таблиц правильно только через него',
    ],
    correctIndex: 1,
    explanation:
      '`UNION` всегда выполняет дедупликацию (сортировку или хэширование всего результата), даже если дубликатов нет — оптимизатор не может этого знать заранее. `UNION ALL` просто дописывает строки и на больших выборках заметно быстрее.',
  },
  {
    type: 'quiz',
    id: 'b4',
    difficulty: 'medium',
    question:
      'Сессия A выполнила `BEGIN; UPDATE accounts SET balance = 0 WHERE id = 1;` и ещё не сделала COMMIT. Что увидит сессия B, выполнив `SELECT balance FROM accounts WHERE id = 1` (уровень READ COMMITTED)?',
    options: [
      'Старое значение баланса — незакоммиченные изменения другим не видны',
      '0 — UPDATE уже выполнен, значит значение изменилось для всех',
      'SELECT зависнет и будет ждать COMMIT или ROLLBACK сессии A',
      'Ошибку сериализации — запрос придётся повторить',
    ],
    correctIndex: 0,
    explanation:
      'Благодаря MVCC читатели не блокируются пишущими: сессия B видит последнюю закоммиченную версию строки, «грязное чтение» в PostgreSQL невозможно ни на одном уровне изоляции. Зависание случилось бы, только если бы B сама попыталась обновить эту же строку.',
  },
  {
    type: 'code',
    id: 'b5',
    difficulty: 'medium',
    title: 'Архивация заказов одним запросом',
    description:
      'Старые заказы (`created_at < \'2023-01-01\'`) нужно перенести из `orders` в таблицу `orders_archive` с той же структурой. Два отдельных запроса (сначала INSERT, потом DELETE) опасны: при сбое между ними строки задвоятся или потеряются. Решите задачу **одним** атомарным запросом через модифицирующий CTE.',
    language: 'sql',
    starterCode: `-- Таблицы orders и orders_archive имеют одинаковую структуру
-- TODO: одним запросом удалите старые заказы из orders
-- и вставьте их в orders_archive
WITH moved AS (
    ...
)
INSERT INTO orders_archive
...;`,
    hints: [
      'DELETE умеет возвращать удалённые строки: DELETE ... RETURNING *.',
      'CTE может содержать не только SELECT: WITH moved AS (DELETE ... RETURNING *) делает удалённые строки доступными дальше по запросу.',
      'Финал: INSERT INTO orders_archive SELECT * FROM moved; — обе операции выполняются в одном операторе, атомарно.',
    ],
    solution: `WITH moved AS (
    DELETE FROM orders
    WHERE created_at < '2023-01-01'
    RETURNING *
)
INSERT INTO orders_archive
SELECT * FROM moved;`,
  },
  {
    type: 'quiz',
    id: 'b6',
    difficulty: 'medium',
    question:
      'Найдите ошибку: запрос `SELECT * FROM users WHERE id NOT IN (SELECT user_id FROM orders)` должен вернуть пользователей без заказов, но всегда возвращает 0 строк. Известно, что в `orders.user_id` есть NULL. В чём причина?',
    options: [
      'NOT IN нельзя использовать с подзапросом — только со списком констант',
      'Подзапрос выполняется заново для каждой строки и не успевает вернуть данные',
      'В подзапросе не хватает DISTINCT — дубликаты ломают сравнение',
      'Из-за NULL в списке условие NOT IN даёт «неизвестно» для каждой строки — нужен NOT EXISTS',
    ],
    correctIndex: 3,
    explanation:
      'Сравнение `x NOT IN (..., NULL)` никогда не бывает истинным: `x <> NULL` даёт UNKNOWN, и вся строка отфильтровывается. `NOT EXISTS` с коррелированным подзапросом свободен от этой ловушки. DISTINCT здесь ни при чём — дубликаты на результат IN не влияют.',
  },
  {
    type: 'code',
    id: 'b7',
    difficulty: 'medium',
    title: 'Дороже среднего в своей категории',
    description:
      'Таблица `products(id, name, category_id, price)`. Выведите `name` и `price` товаров, у которых цена **выше средней цены в их категории**. Сравнивать нужно не с общим средним по таблице, а со средним именно той категории, к которой относится товар.',
    language: 'sql',
    starterCode: `-- TODO: товары дороже среднего по своей категории.
-- Подсказка: среднее для каждой категории можно посчитать
-- коррелированным подзапросом
SELECT p.name, p.price
FROM products p
WHERE ...;`,
    hints: [
      'Внутренний запрос должен считать AVG(price) не по всей таблице, а только по строкам с той же category_id, что у внешней строки.',
      'Свяжите подзапрос с внешним запросом условием `WHERE category_id = p.category_id` — это и называется коррелированным подзапросом.',
      'Альтернатива: оконная функция `AVG(price) OVER (PARTITION BY category_id)` во вложенном SELECT и фильтр по ней снаружи.',
    ],
    solution: `SELECT p.name, p.price
FROM products p
WHERE p.price > (
    SELECT AVG(price)
    FROM products
    WHERE category_id = p.category_id
);`,
  },
  {
    type: 'quiz',
    id: 'b8',
    difficulty: 'medium',
    question: 'Чем тип `jsonb` в PostgreSQL отличается от `json`?',
    options: [
      'Ничем — jsonb это просто новое имя типа json в свежих версиях',
      'jsonb хранит разобранное бинарное представление: медленнее запись, зато быстрые операторы и GIN-индексы; порядок ключей и дубликаты не сохраняются',
      'jsonb быстрее записывается, а json быстрее читается и фильтруется',
      'json проверяет синтаксис документа, а jsonb принимает любые строки без проверки',
    ],
    correctIndex: 1,
    explanation:
      '`jsonb` разбирает документ при записи в бинарную форму — поэтому вставка чуть дороже, зато поиск по операторам `@>`, `->>` быстрый и поддерживаются GIN-индексы. `json` хранит исходный текст как есть. Вариант «jsonb быстрее пишется, json быстрее читается» — распространённая путаница с точностью до наоборот.',
  },
  {
    type: 'quiz',
    id: 'b9',
    difficulty: 'medium',
    question:
      'Вы проектируете FK `orders.user_id`. Пользователь может удалить свой аккаунт, но заказы нужны бухгалтерии ещё много лет. Какой вариант лучший?',
    options: [
      'ON DELETE CASCADE — база сама наведёт порядок при удалении пользователя',
      'ON DELETE SET DEFAULT — заказы перепишутся на служебного пользователя',
      'ON DELETE RESTRICT плюс soft delete пользователя — жёсткое удаление запрещено, история заказов сохраняется',
      'Убрать внешний ключ — тогда удаление пользователя ничего не сломает',
    ],
    correctIndex: 2,
    explanation:
      'CASCADE здесь — опасный антипаттерн: удаление аккаунта молча уничтожит финансовую историю. RESTRICT не даст случайно удалить пользователя с заказами, а «удалением» аккаунта становится пометка `deleted_at` — данные остаются целыми и связными.',
  },
  {
    type: 'code',
    id: 'b10',
    difficulty: 'medium',
    title: 'Последний заказ каждого клиента',
    description:
      'Таблица `orders(id, user_id, created_at, total)`. Для каждого `user_id` выведите его самый свежий заказ: `id`, `user_id`, `created_at`, `total`. Решите через оконную функцию — `GROUP BY` тут не поможет, потому что нужны все колонки строки, а не только агрегаты.',
    language: 'sql',
    starterCode: `-- TODO: пронумеруйте заказы каждого пользователя от новых к старым
-- и оставьте только первый в каждой группе
SELECT id, user_id, created_at, total
FROM (
    ...
) t
WHERE ...;`,
    hints: [
      'Оконная функция ROW_NUMBER() умеет нумеровать строки внутри групп: `OVER (PARTITION BY user_id ORDER BY ...)`.',
      'Сортируйте внутри окна по `created_at DESC`, тогда самый свежий заказ получит номер 1.',
      'Оконные функции нельзя использовать в WHERE напрямую — оберните запрос в подзапрос и фильтруйте `WHERE rn = 1`. В PostgreSQL есть и короткий путь: `SELECT DISTINCT ON (user_id) ... ORDER BY user_id, created_at DESC`.',
    ],
    solution: `SELECT id, user_id, created_at, total
FROM (
    SELECT
        id, user_id, created_at, total,
        ROW_NUMBER() OVER (
            PARTITION BY user_id
            ORDER BY created_at DESC
        ) AS rn
    FROM orders
) t
WHERE rn = 1;`,
  },
  {
    type: 'quiz',
    id: 'b11',
    difficulty: 'medium',
    question:
      'Django-код: `qs = Order.objects.filter(status=\'paid\')`, затем `first = qs[0]`, затем `for o in qs: ...`. Сколько SQL-запросов уйдёт в базу?',
    options: [
      'Два: срез `qs[0]` выполняет отдельный запрос с LIMIT 1, обход в цикле — второй, полный',
      'Один: после `qs[0]` весь результат уже закеширован в QuerySet',
      'Три: по одному на filter, срез и цикл',
      'Ни одного: QuerySet ленивый и выполняется только при сериализации в ответ',
    ],
    correctIndex: 0,
    explanation:
      'Сам `filter()` запрос не выполняет. Срез невычисленного QuerySet превращается в отдельный запрос с `LIMIT 1` и **не** наполняет кэш результата; полный кэш появляется только при итерации — это второй запрос. Заблуждение «один запрос» опасно: код с несколькими срезами незаметно плодит обращения к базе.',
  },
  {
    type: 'quiz',
    id: 'b12',
    difficulty: 'medium',
    question:
      'Backend-приложение под нагрузкой открывает новое подключение к PostgreSQL на каждый HTTP-запрос и закрывает его после ответа. Чем это плохо и как делают правильно?',
    options: [
      'Ничем — установка соединения в PostgreSQL практически бесплатна',
      'Плохо только тем, что база логирует каждое подключение и лог быстро растёт',
      'PostgreSQL не разрешает повторные подключения с одного хоста чаще раза в секунду',
      'Каждое подключение — это TCP-handshake, аутентификация и отдельный процесс на сервере БД; вместо этого держат пул соединений (пул драйвера или PgBouncer)',
    ],
    correctIndex: 3,
    explanation:
      'В PostgreSQL под каждое соединение форкается отдельный серверный процесс, плюс тратится время на сеть и аутентификацию — под нагрузкой эти накладные расходы съедают заметную долю времени ответа и упираются в лимит max_connections. Пул соединений открывает их заранее и выдаёт запросам по очереди, многократно переиспользуя.',
  },
  {
    type: 'code',
    id: 'b13',
    difficulty: 'hard',
    title: 'Пагинация без OFFSET',
    description:
      'Запрос `SELECT id, total FROM orders ORDER BY id DESC LIMIT 20 OFFSET 100000` на глубоких страницах тормозит: база читает и выбрасывает 100 000 строк. Перепишите его на **keyset-пагинацию**: клиент передаёт id последнего увиденного заказа (например, 500000), нужно вернуть следующие 20 более старых заказов.',
    language: 'sql',
    starterCode: `-- Медленный вариант:
-- SELECT id, total FROM orders ORDER BY id DESC LIMIT 20 OFFSET 100000;

-- TODO: keyset-пагинация — следующая страница после id = 500000
SELECT id, total
FROM orders
...;`,
    hints: [
      'Идея keyset: вместо «пропусти N строк» — «продолжи с того места, где остановились». Позицию задаёт значение ключа сортировки с прошлой страницы.',
      'Сортировка по id DESC означает, что следующая страница — это строки с id **меньше** последнего увиденного.',
      'Условие `WHERE id < 500000` плюс тот же `ORDER BY id DESC LIMIT 20`: индекс по id сразу отдаёт нужные 20 строк без чтения предыдущих страниц.',
    ],
    solution: `SELECT id, total
FROM orders
WHERE id < 500000
ORDER BY id DESC
LIMIT 20;`,
  },
  {
    type: 'quiz',
    id: 'b14',
    difficulty: 'hard',
    question:
      'Две транзакции параллельно выполняют «SELECT баланса → прибавить 100 в коде → UPDATE» для счёта с балансом 0 (READ COMMITTED). Какой итог возможен и как это чинится?',
    options: [
      'Всегда 200: база автоматически сериализует конкурирующие записи',
      'Возможен итог 100 — потерянное обновление; лечится атомарным `UPDATE ... SET balance = balance + 100` или SELECT ... FOR UPDATE',
      'Возможен итог 0: обе транзакции откатятся из-за конфликта записи',
      'Будет deadlock, и обе транзакции зависнут до перезапуска сервера',
    ],
    correctIndex: 1,
    explanation:
      'Обе транзакции могут прочитать 0, посчитать 100 и записать 100 — второе обновление затирает первое, это классический lost update. READ COMMITTED от него не защищает: обычный SELECT ничего не блокирует. Решение — сложение на стороне SQL одной командой либо явная блокировка строки FOR UPDATE.',
  },
  {
    type: 'code',
    id: 'b15',
    difficulty: 'hard',
    title: 'Поиск по jsonb с GIN-индексом',
    description:
      'В таблице `events(id, payload jsonb)` миллионы строк. Запрос «события, где в payload поле type равно \'click\'» выполняется Seq Scan-ом: обычный B-tree по jsonb-колонке тут не помогает. Создайте подходящий **GIN-индекс** и напишите запрос с оператором содержания `@>`, который сможет этим индексом воспользоваться.',
    language: 'sql',
    starterCode: `-- Таблица: events (id bigint, payload jsonb)
-- TODO 1: создайте GIN-индекс по payload

-- TODO 2: выберите id событий, где payload содержит
-- пару "type": "click", — оператором @>
SELECT id FROM events WHERE ...;`,
    hints: [
      'B-tree сравнивает значения целиком и не умеет искать «внутри» jsonb-документа; для составных значений в PostgreSQL есть GIN-индексы.',
      'Синтаксис: CREATE INDEX ... ON events USING gin (payload);',
      'GIN поддерживает оператор содержания: payload @> \'{"type": "click"}\'. А вот условие payload->>\'type\' = \'click\' таким индексом не воспользуется — это выражение, а не оператор содержания.',
    ],
    solution: `-- GIN индексирует все ключи и значения внутри каждого документа
CREATE INDEX events_payload_gin
    ON events USING gin (payload);

-- Оператор @> («документ содержит указанный фрагмент»)
-- умеет пользоваться GIN-индексом
SELECT id
FROM events
WHERE payload @> '{"type": "click"}';`,
  },
  {
    type: 'quiz',
    id: 'b16',
    difficulty: 'hard',
    question:
      'В таблице 10 млн строк; 95% значений колонки `status` — `\'done\'`. Есть B-tree индекс по `status`. Почему план для `WHERE status = \'done\'` показывает Seq Scan, а для `WHERE status = \'new\'` — Index Scan?',
    options: [
      'Индекс частично повреждён и работает только для редких значений',
      'Seq Scan — это и есть чтение через индекс, просто другое название в плане',
      'Планировщик по статистике видит: ради 95% строк дешевле прочитать таблицу подряд, чем прыгать по индексу к каждой строке',
      'B-tree в принципе не умеет искать часто встречающиеся значения',
    ],
    correctIndex: 2,
    explanation:
      'Index Scan — это случайные чтения: за каждой найденной записью индекс отправляет базу в кучу таблицы. Когда подходит почти вся таблица, последовательное чтение дешевле, и планировщик сознательно игнорирует индекс — это не поломка, а расчёт стоимости по собранной статистике распределения значений.',
  },
  {
    type: 'code',
    id: 'b17',
    difficulty: 'hard',
    title: 'Уникальный email при soft delete',
    description:
      'В таблице `users` есть колонка `deleted_at TIMESTAMPTZ` (NULL — пользователь активен). Обычный UNIQUE по email мешает жить: удалённый год назад пользователь навсегда блокирует повторную регистрацию с тем же адресом. Создайте индекс, который гарантирует уникальность email **только среди активных** пользователей.',
    language: 'sql',
    starterCode: `-- Обычный UNIQUE не подходит:
-- ALTER TABLE users ADD CONSTRAINT users_email_uniq UNIQUE (email);

-- TODO: уникальность только для строк, где deleted_at IS NULL`,
    hints: [
      'Вспомните partial-индексы: у индекса может быть собственное условие WHERE, и тогда он покрывает только часть строк.',
      'Индекс бывает не только ускорителем, но и ограничением: CREATE UNIQUE INDEX работает как UNIQUE-constraint для строк, попавших под условие.',
      'Соедините обе идеи: `CREATE UNIQUE INDEX ... ON users (email) WHERE deleted_at IS NULL`.',
    ],
    solution: `-- Уникальность проверяется только среди активных пользователей:
-- строки с deleted_at IS NOT NULL в индекс не попадают вовсе
CREATE UNIQUE INDEX users_email_active_uniq
    ON users (email)
    WHERE deleted_at IS NULL;`,
  },
  {
    type: 'quiz',
    id: 'b18',
    difficulty: 'hard',
    question:
      'Запрос работает несколько минут. В плане: `Nested Loop ... (rows=2) (actual rows=180000)`. Что стоит сделать в первую очередь?',
    options: [
      'Выполнить ANALYZE: планировщик выбрал Nested Loop из-за устаревшей статистики, ожидая 2 строки вместо 180 тысяч',
      'Добавить больше индексов — Nested Loop всегда означает их нехватку',
      'Увеличить work_mem: планировщику не хватило памяти на построение правильного плана',
      'Переписать запрос без JOIN — соединения в SQL принципиально медленные',
    ],
    correctIndex: 0,
    explanation:
      'Разрыв между оценкой `rows=2` и фактом `actual rows=180000` — маркер устаревшей или недостаточной статистики: Nested Loop прекрасен для пары строк и катастрофичен для сотен тысяч. После ANALYZE планировщик, скорее всего, выберет Hash Join. Новые индексы не помогут, пока оценки строк ошибаются на пять порядков.',
  },
  {
    type: 'quiz',
    id: 'b19',
    difficulty: 'hard',
    question:
      'Транзакция A обновила строку 1 и хочет обновить строку 2; транзакция B уже обновила строку 2 и хочет строку 1. Обе ждут друг друга. Что сделает PostgreSQL?',
    options: [
      'Обе зависнут навсегда — разруливать придётся перезапуском сервера',
      'Обе выполнятся: MVCC позволяет обновлять одни и те же строки параллельно',
      'Обе откатятся автоматически, и данные вернутся к исходному состоянию',
      'Примерно через секунду детектор взаимоблокировок оборвёт одну транзакцию с ошибкой deadlock, вторая продолжит работу',
    ],
    correctIndex: 3,
    explanation:
      'PostgreSQL периодически (по умолчанию раз в `deadlock_timeout` = 1 с) ищет циклы ожидания и жертвует одной из транзакций — та получает ошибку `deadlock detected`, вторая спокойно завершается. Профилактика на стороне приложения: захватывать строки во всех транзакциях в одном и том же порядке, например по возрастанию id.',
  },
  {
    type: 'code',
    id: 'b20',
    difficulty: 'hard',
    title: 'Очередь задач на SKIP LOCKED',
    description:
      'Таблица `jobs(id, status, payload)` — очередь фоновых задач, из которой читают несколько воркеров одновременно. Напишите запрос, который атомарно забирает **одну** задачу со `status = \'queued\'`: помечает её как `\'processing\'` и возвращает `id` и `payload`. Воркеры не должны брать одну и ту же задачу и не должны ждать друг друга.',
    language: 'sql',
    starterCode: `-- TODO: атомарно захватить одну свободную задачу.
-- Подсказка: подзапрос с блокировкой строки + UPDATE ... RETURNING
UPDATE jobs
SET status = 'processing'
WHERE id = (
    ...
)
RETURNING id, payload;`,
    hints: [
      'Строку-кандидата нужно заблокировать при выборе: `SELECT ... FOR UPDATE`, иначе два воркера выберут одну и ту же задачу.',
      'Обычный FOR UPDATE заставит второго воркера ждать первого. Модификатор SKIP LOCKED велит пропускать уже захваченные строки и брать следующую свободную.',
      'Итог: подзапрос `SELECT id FROM jobs WHERE status = \'queued\' ORDER BY id FOR UPDATE SKIP LOCKED LIMIT 1` внутри UPDATE ... RETURNING.',
    ],
    solution: `UPDATE jobs
SET status = 'processing'
WHERE id = (
    SELECT id
    FROM jobs
    WHERE status = 'queued'
    ORDER BY id
    FOR UPDATE SKIP LOCKED  -- занятые строки пропускаем, не ждём
    LIMIT 1
)
RETURNING id, payload;`,
  },
  {
    type: 'quiz',
    id: 'b21',
    difficulty: 'hard',
    question:
      'Вам нужно добавить индекс на таблицу в 200 млн строк на проде, не останавливая запись. Как правильно?',
    options: [
      'Обычный CREATE INDEX — он и так не блокирует ни чтение, ни запись',
      'CREATE INDEX CONCURRENTLY: запись не блокируется, но команда работает дольше и её нельзя выполнять внутри транзакции',
      'Скопировать таблицу в новую с готовым индексом и удалить старую',
      'REINDEX — он в любой версии создаёт индексы онлайн без блокировок',
    ],
    correctIndex: 1,
    explanation:
      'Обычный `CREATE INDEX` берёт блокировку, запрещающую INSERT/UPDATE/DELETE на всё время построения — на большой таблице это простой на минуты или часы. `CONCURRENTLY` строит индекс в два прохода без блокировки записи, но требует отдельного соединения вне транзакции — в Alembic для этого используют autocommit-блок.',
  },
  {
    type: 'code',
    id: 'b22',
    difficulty: 'hard',
    title: 'Агрегация на стороне базы',
    description:
      'Код собирает словарь «id пользователя → число его заказов», обходя всех пользователей в цикле: это N+1 запросов и лишние объекты в памяти. Перепишите так, чтобы в базу ушёл **один** SQL-запрос с GROUP BY, а Python получил уже готовые пары значений (Django ORM).',
    language: 'python',
    starterCode: `# Медленно: 1 запрос за пользователями + по запросу на каждого
result = {}
for user in User.objects.all():
    result[user.id] = user.orders.count()

# TODO: тот же словарь одним SQL-запросом через annotate`,
    hints: [
      'annotate() добавляет каждому объекту вычисляемое поле; с агрегатом Count по связи он превращается в SELECT ... GROUP BY на стороне базы.',
      'Число заказов пользователя: `User.objects.annotate(cnt=Count(\'orders\'))` — related name связи передаётся строкой.',
      'Чтобы не создавать модельные объекты, заберите только нужные поля: `.values_list(\'id\', \'cnt\')` — и оберните результат в dict().',
    ],
    solution: `from django.db.models import Count

# Один запрос: SELECT ... LEFT JOIN orders ... GROUP BY users.id
result = dict(
    User.objects
    .annotate(cnt=Count('orders'))
    .values_list('id', 'cnt')
)`,
  },
  {
    type: 'quiz',
    id: 'b23',
    difficulty: 'hard',
    question:
      'Таблица занимает на диске 20 ГБ, хотя живых данных в ней около 2 ГБ. Недавно в ней миллионами строк обновляли статусы. Почему так вышло и кто это чинит?',
    options: [
      'UPDATE переписывает строку на месте — дело не в обновлениях, просто слишком много индексов',
      'PostgreSQL никогда не освобождает занятое место, поможет только пересоздание базы с нуля',
      'MVCC: каждый UPDATE создаёт новую версию строки, старые версии остаются «мёртвыми»; autovacuum помечает их место как переиспользуемое',
      'Это дисковый кэш запросов — он очистится сам при перезапуске сервера',
    ],
    correctIndex: 2,
    explanation:
      'В PostgreSQL UPDATE — это по сути INSERT новой версии строки плюс пометка старой: массовые обновления порождают горы мёртвых версий (bloat). Autovacuum освобождает их место для новых записей, но файл при этом не сжимается — вернуть место ОС могут только `VACUUM FULL` (с блокировкой) или `pg_repack`.',
  },
  {
    type: 'code',
    id: 'b24',
    difficulty: 'hard',
    title: 'Дерево категорий рекурсивным CTE',
    description:
      'Таблица `categories(id, parent_id, name)`; у корневых категорий `parent_id IS NULL`. Выведите **все** подкатегории категории с id = 5 на любую глубину вложенности: `id`, `name` и уровень `depth` (прямые дети — 1, их дети — 2 и т.д.). Саму категорию 5 в результат не включайте.',
    language: 'sql',
    starterCode: `-- TODO: рекурсивный обход дерева вниз от категории 5
WITH RECURSIVE subtree AS (
    -- якорь: стартовая категория

    UNION ALL

    -- шаг: дети уже найденных категорий

)
SELECT id, name, depth
FROM subtree
...;`,
    hints: [
      'Рекурсивный CTE состоит из якоря (стартовые строки) и шага, который через JOIN к самому CTE находит детей уже найденных строк.',
      'Заведите колонку глубины: в якоре `0 AS depth`, на шаге — `s.depth + 1`. Шаг соединяет `categories c JOIN subtree s ON c.parent_id = s.id`.',
      'Якорь — сама категория 5 (`WHERE id = 5`), поэтому в финальном SELECT исключите её: `WHERE id <> 5` (или фильтром по depth > 0).',
    ],
    solution: `WITH RECURSIVE subtree AS (
    SELECT id, parent_id, name, 0 AS depth
    FROM categories
    WHERE id = 5

    UNION ALL

    SELECT c.id, c.parent_id, c.name, s.depth + 1
    FROM categories c
    JOIN subtree s ON c.parent_id = s.id
)
SELECT id, name, depth
FROM subtree
WHERE depth > 0
ORDER BY depth, id;`,
  },
  {
    type: 'quiz',
    id: 'b25',
    difficulty: 'hard',
    question:
      'При каких условиях планировщик сможет выполнить **Index Only Scan** — отдать результат прямо из индекса, вообще не обращаясь к таблице?',
    options: [
      'Все нужные запросу колонки лежат в индексе (в ключе или INCLUDE), а карта видимости подтверждает, что страницы не менялись с последнего VACUUM',
      'Достаточно любого индекса по колонке, упомянутой в WHERE',
      'Только если вся таблица целиком помещается в оперативную память',
      'Index Only Scan включается параметром в postgresql.conf',
    ],
    correctIndex: 0,
    explanation:
      'Нужны два условия сразу: индекс покрывает все колонки запроса, и visibility map показывает страницы «всем видимыми» — иначе базе придётся ходить в кучу проверять версии строк (в плане это видно как Heap Fetches). Поэтому сразу после массовых обновлений покрывающий индекс может не давать выигрыша, пока не отработает VACUUM.',
  },
  {
    type: 'quiz',
    id: 'b26',
    difficulty: 'hard',
    question:
      'В таблице на сотни миллионов строк первичный ключ — UUID v4. Вставки со временем замедлились, индекс распух. В чём «подкапотная» причина?',
    options: [
      'UUID хранится строкой из 36 символов — отсюда размер и медленный поиск',
      'PostgreSQL не умеет индексировать UUID напрямую и строит индекс по хэшу',
      'При UUID-ключах база блокирует всю таблицу на каждую вставку',
      'Случайные значения попадают в случайные страницы B-tree: кэш почти не помогает, страницы делятся чаще; лечится последовательными ключами — IDENTITY или UUIDv7',
    ],
    correctIndex: 3,
    explanation:
      'UUID v4 полностью случаен, поэтому каждая вставка бьёт в произвольное место B-tree: горячих страниц нет, кэш неэффективен, page split-ы раздувают индекс. Последовательные ключи всегда дописывают в правый край дерева. Вариант про строку неверен: тип `uuid` хранится компактно, 16 байт.',
  },
  {
    type: 'code',
    id: 'b27',
    difficulty: 'hard',
    title: 'INT -> BIGINT без даунтайма',
    description:
      'Колонка `orders.amount_cents INT` скоро переполнится. Прямой `ALTER TABLE orders ALTER COLUMN amount_cents TYPE BIGINT` перепишет всю таблицу под эксклюзивной блокировкой — прод ляжет. Напишите последовательность SQL-шагов безопасной миграции: новая колонка, бэкфилл порциями, быстрое переключение. Считайте, что на время миграции приложение пишет в обе колонки.',
    language: 'sql',
    starterCode: `-- Шаг 1. TODO: добавить новую BIGINT-колонку (мгновенная операция)


-- Шаг 2. TODO: перенести данные порциями, не блокируя таблицу надолго
-- (шаблон одной порции; выполняется в цикле скриптом деплоя)


-- Шаг 3. TODO: в одной короткой транзакции поменять колонки местами`,
    hints: [
      'ALTER TABLE ... ADD COLUMN без DEFAULT-выражения с перезаписью — мгновенная операция: меняется только каталог, строки не трогаются.',
      'Бэкфилл одним UPDATE на всю таблицу — та же долгая блокировка и распухание от мёртвых версий. Обновляйте диапазонами по id (например, по 10 000 строк) с паузами.',
      'Финал — короткая транзакция из двух RENAME COLUMN: старую колонку в сторону, новую на её место. Блокировка нужна лишь на миллисекунды.',
    ],
    solution: `-- Шаг 1. Новая колонка: мгновенно, таблица не переписывается
ALTER TABLE orders ADD COLUMN amount_cents_big BIGINT;

-- Шаг 2. Бэкфилл порциями (цикл во внешнем скрипте меняет диапазоны):
UPDATE orders
SET amount_cents_big = amount_cents
WHERE id >= 1 AND id < 10001
  AND amount_cents_big IS NULL;
-- ...следующая порция: id >= 10001 AND id < 20001 и т.д.

-- Шаг 3. Переключение: две быстрые операции над каталогом
BEGIN;
ALTER TABLE orders RENAME COLUMN amount_cents TO amount_cents_old;
ALTER TABLE orders RENAME COLUMN amount_cents_big TO amount_cents;
COMMIT;
-- Старую колонку удаляем отдельным релизом, убедившись, что всё работает`,
  },
  {
    type: 'quiz',
    id: 'b28',
    difficulty: 'hard',
    question:
      'Вы перевели критичные операции на уровень изоляции SERIALIZABLE. В логах приложения появились ошибки `40001 serialization_failure`. Это баг?',
    options: [
      'Да, SERIALIZABLE в PostgreSQL работает нестабильно — нужно вернуться на READ COMMITTED',
      'Нет, это штатный механизм: обнаружив конфликт, база обрывает одну из транзакций, а приложение обязано выполнить её заново',
      'Да, эта ошибка означает deadlock — надо менять порядок захвата блокировок',
      'Нет, ошибку можно молча игнорировать — данные транзакции всё равно записались',
    ],
    correctIndex: 1,
    explanation:
      'SERIALIZABLE не блокирует конфликтующие транзакции заранее, а отслеживает зависимости и обрывает ту, что нарушает сериализуемость, — код `40001` прямо означает «повтори транзакцию целиком». Игнорировать её нельзя: транзакция откатилась, и без ретрая операция будет потеряна.',
  },
  {
    type: 'code',
    id: 'b29',
    difficulty: 'hard',
    title: 'Ретрай транзакции при serialization_failure',
    description:
      'Допишите функцию, которая выполняет переданную операцию в транзакции уровня SERIALIZABLE и при ошибке сериализации автоматически повторяет её — всего до 3 попыток (psycopg 3). Если все попытки исчерпаны, ошибка должна уйти наверх.',
    language: 'python',
    starterCode: `import psycopg
from psycopg import errors

def run_serializable(conn, operation, retries=3):
    # TODO: выполнить operation(conn) внутри SERIALIZABLE-транзакции;
    # при errors.SerializationFailure — откатиться и повторить,
    # после последней неудачной попытки пробросить исключение
    ...`,
    hints: [
      'Контекстный менеджер `with conn.transaction():` открывает транзакцию, коммитит при выходе и откатывает при исключении.',
      'Уровень задаётся первой командой транзакции: `conn.execute(\'SET TRANSACTION ISOLATION LEVEL SERIALIZABLE\')`.',
      'Оберните тело в цикл `for attempt in range(retries)` и ловите `errors.SerializationFailure`; если попытка последняя — `raise`, иначе продолжайте цикл.',
    ],
    solution: `import psycopg
from psycopg import errors

def run_serializable(conn, operation, retries=3):
    for attempt in range(1, retries + 1):
        try:
            with conn.transaction():  # commit на выходе, rollback при ошибке
                conn.execute(
                    "SET TRANSACTION ISOLATION LEVEL SERIALIZABLE"
                )
                return operation(conn)
        except errors.SerializationFailure:
            if attempt == retries:
                raise  # попытки кончились — отдаём ошибку наверх
            # конфликт сериализации: транзакция уже откатилась,
            # безопасно пробуем ещё раз с чистого листа`,
  },
  {
    type: 'quiz',
    id: 'b30',
    difficulty: 'hard',
    question:
      'Отчётный запрос с ORDER BY по 5 млн строк работает медленно. В EXPLAIN ANALYZE: `Sort Method: external merge  Disk: 840MB`. Что это значит и какой вариант лучший?',
    options: [
      'Диск неисправен — сортировка не должна обращаться к нему вообще',
      'База сортирует индекс, а не данные; нужно удалить индекс',
      'Сортировке не хватило work_mem, и она ушла на диск; помогут увеличение work_mem для этого запроса или индекс по колонке сортировки, снимающий Sort из плана',
      'external merge — самый быстрый метод сортировки, менять ничего не нужно',
    ],
    correctIndex: 2,
    explanation:
      '`external merge Disk` означает, что данные не поместились в память (`work_mem`) и сортировка шла через временные файлы — на порядки медленнее. Точечно поднять `work_mem` в сессии отчёта или дать планировщику индекс в порядке сортировки (тогда Sort исчезает из плана) — рабочие решения; «удалить индекс» проблему только усугубит.',
  },
]
